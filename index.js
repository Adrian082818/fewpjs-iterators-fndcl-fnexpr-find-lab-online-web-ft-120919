const testVar = {}

function testFunc() {
  return "hi"
}

function superbowlWin(array) {
  let result 
 array.find(function(obj) {
  if (obj.result === "W") {
    result = obj.year 
  }
})
return result 
};

